# Backend

Backend Node.js/PostgreSQL độc lập, không chứa UI.

- `server.mjs`: HTTP API, auth/session, RBAC và nghiệp vụ.
- `migrate.mjs`: chạy migration schema.
- `schema.postgres.sql`: schema PostgreSQL nguồn chuẩn.
- `README.md`: hướng dẫn chạy và API contract.
